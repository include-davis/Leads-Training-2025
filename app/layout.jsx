import './(pages)/_globals/globals.scss';
import { ThemeProvider } from './(pages)/_context/ThemeContext';
import ThemeSwitcher from './(pages)/_components/ThemeSwitcher/ThemeSwitcher';
import Header from './(pages)/_components/Header/Header';
import Footer from './(pages)/_components/Footer/Footer';

export const metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>
        <ThemeProvider>
          {/* Theme switcher always in top-right */}
          <div style={{ display: 'flex', justifyContent: 'flex-end', padding: '1rem' }}>
            <ThemeSwitcher />
          </div>

          <Header />
          {children}
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  );
}
